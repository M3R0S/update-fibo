import { FC } from 'react';

import { {{pascalCase}}Props } from '.';

export const {{pascalCase}}: FC<{{pascalCase}}Props> = (props) => {
    const {  } = props

	return (
        <div>
            
        </div>
    );
};
